<Window x:Class="SmartIDReader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SmartID Card Reader"
        Width="460" Height="480"
        MinWidth="460" MinHeight="480"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen"
        Background="#F3F3F3">

    <Window.Resources>
        <!-- ─── Bảng màu Fluent UI ─── -->
        <SolidColorBrush x:Key="BgBrush" Color="#F3F3F3"/>
        <SolidColorBrush x:Key="CardBgBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#1A1A1A"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#5F5F5F"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#E5E5E5"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#0067C0"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#0F7B0F"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#C42B1C"/>

        <!-- ─── Modern TextBox Style ─── -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="6,3"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <!-- ─── Modern ComboBox Style ─── -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="6,3"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Background" Value="#FFFFFF"/>
        </Style>

        <!-- ─── Modern Button Style ─── -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F6F6F6"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#EEEEEE"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#E0E0E0"/>
                                <Setter Property="Foreground" Value="#888888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Status & Quick Connect Card -->
            <RowDefinition Height="Auto"/> <!-- Advanced Config (Always Visible) -->
            <RowDefinition Height="*"/>    <!-- Terminal / Logs -->
        </Grid.RowDefinitions>

        <!-- ══════════════ CARD CHÍNH ══════════════ -->
        <Border Grid.Row="0"
                Background="{StaticResource CardBgBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="12">
            <Grid>
                <!-- Cột duy nhất, chiếm toàn bộ bề ngang để hiển thị rộng rãi -->
                <StackPanel VerticalAlignment="Center">
                    <DockPanel LastChildFill="True" Margin="0,0,0,6">
                        <TextBlock Text="Cổng kết nối:"
                                   FontSize="13"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   VerticalAlignment="Center"
                                   Width="90"
                                   DockPanel.Dock="Left"/>
                        <ComboBox x:Name="CbComPort"
                                  Style="{StaticResource ModernComboBox}"
                                  SelectionChanged="CbComPort_SelectionChanged"
                                  ToolTip="Chọn cổng COM của thiết bị đọc CCCD"/>
                    </DockPanel>

                    <!-- Trạng thái kết nối -->
                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                        <Ellipse x:Name="StatusDot"
                                 Width="8" Height="8"
                                 Fill="{StaticResource DangerBrush}"
                                 VerticalAlignment="Center"
                                 Margin="2,0,8,0"/>
                        <TextBlock x:Name="StatusMsg"
                                   Text="Đang kết nối..."
                                   FontSize="12"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis"/>
                        <TextBlock x:Name="AdminBadge"
                                   Text=" (Admin)"
                                   FontSize="11"
                                   Foreground="{StaticResource AccentBrush}"
                                   FontWeight="Bold"
                                   VerticalAlignment="Center"
                                   Visibility="Collapsed"
                                   Margin="6,0,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ══════════════ CẤU HÌNH NÂNG CAO (HIỂN THỊ FULL MẶC ĐỊNH) ══════════════ -->
        <Border x:Name="PanelAdvanced"
                Grid.Row="1"
                Visibility="Visible"
                Background="{StaticResource CardBgBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="12"
                Margin="0,10,0,0">
            <StackPanel>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Hàng 1: Baudrate & Databits -->
                    <DockPanel Grid.Row="0" Grid.Column="0" Margin="0,0,8,6">
                        <TextBlock Text="Baud Rate:" VerticalAlignment="Center" Width="70" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <ComboBox x:Name="CbBaudRate" Style="{StaticResource ModernComboBox}" SelectionChanged="Advanced_SelectionChanged">
                            <ComboBoxItem Content="1200"/>
                            <ComboBoxItem Content="2400"/>
                            <ComboBoxItem Content="4800"/>
                            <ComboBoxItem Content="9600" IsSelected="True"/>
                            <ComboBoxItem Content="19200"/>
                            <ComboBoxItem Content="38400"/>
                            <ComboBoxItem Content="57600"/>
                            <ComboBoxItem Content="115200"/>
                        </ComboBox>
                    </DockPanel>

                    <DockPanel Grid.Row="0" Grid.Column="1" Margin="8,0,0,6">
                        <TextBlock Text="Data Bits:" VerticalAlignment="Center" Width="70" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <ComboBox x:Name="CbDataBits" Style="{StaticResource ModernComboBox}" SelectionChanged="Advanced_SelectionChanged">
                            <ComboBoxItem Content="5"/>
                            <ComboBoxItem Content="6"/>
                            <ComboBoxItem Content="7"/>
                            <ComboBoxItem Content="8" IsSelected="True"/>
                        </ComboBox>
                    </DockPanel>

                    <!-- Hàng 2: Parity & Stopbits -->
                    <DockPanel Grid.Row="1" Grid.Column="0" Margin="0,0,8,6">
                        <TextBlock Text="Parity:" VerticalAlignment="Center" Width="70" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <ComboBox x:Name="CbParity" Style="{StaticResource ModernComboBox}" SelectionChanged="Advanced_SelectionChanged">
                            <ComboBoxItem Content="none" IsSelected="True"/>
                            <ComboBoxItem Content="even"/>
                            <ComboBoxItem Content="odd"/>
                            <ComboBoxItem Content="mark"/>
                            <ComboBoxItem Content="space"/>
                        </ComboBox>
                    </DockPanel>

                    <DockPanel Grid.Row="1" Grid.Column="1" Margin="8,0,0,6">
                        <TextBlock Text="Stop Bits:" VerticalAlignment="Center" Width="70" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <ComboBox x:Name="CbStopBits" Style="{StaticResource ModernComboBox}" SelectionChanged="Advanced_SelectionChanged">
                            <ComboBoxItem Content="1" IsSelected="True"/>
                            <ComboBoxItem Content="1.5"/>
                            <ComboBoxItem Content="2"/>
                        </ComboBox>
                    </DockPanel>

                    <!-- Hàng 3: Encoding & Autoreconnect -->
                    <DockPanel Grid.Row="2" Grid.Column="0" Margin="0,0,8,0">
                        <TextBlock Text="Encoding:" VerticalAlignment="Center" Width="70" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <ComboBox x:Name="CbEncoding" Style="{StaticResource ModernComboBox}" SelectionChanged="Advanced_SelectionChanged">
                            <ComboBoxItem Content="utf8" IsSelected="True"/>
                            <ComboBoxItem Content="ascii"/>
                        </ComboBox>
                    </DockPanel>

                    <!-- Khởi động cùng Windows -->
                    <CheckBox x:Name="ChkAutoStart"
                              Grid.Row="2" Grid.Column="1"
                              Content="Khởi động cùng Windows"
                              FontSize="12"
                              Foreground="{StaticResource TextSecondaryBrush}"
                              VerticalAlignment="Center"
                              Margin="12,0,0,0"
                              Checked="AutoStart_Changed"
                              Unchecked="AutoStart_Changed"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- ══════════════ TERMINAL (LOGS) ══════════════ -->
        <Border Grid.Row="2"
                Background="#1E1E1E"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="10"
                Margin="0,10,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Terminal Header -->
                <DockPanel Grid.Row="0" LastChildFill="True" Margin="0,0,0,6">
                    <Button Content="Xóa log" 
                            Click="BtnClearLog_Click"
                            DockPanel.Dock="Right"
                            Height="20"
                            Padding="8,0"
                            FontSize="10"
                            Background="#2D2D2D"
                            Foreground="#E0E0E0"
                            BorderBrush="#444444"
                            Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="3">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <TextBlock Text="Terminal Logs"
                               FontFamily="Consolas"
                               FontWeight="Bold"
                               FontSize="11"
                               Foreground="#888888"
                               VerticalAlignment="Center"/>
                </DockPanel>
                
                <!-- Terminal TextBox -->
                <TextBox x:Name="TxtTerminal"
                         Grid.Row="1"
                         Background="Transparent"
                         Foreground="#A9FFAC"
                         BorderThickness="0"
                         FontFamily="Consolas"
                         FontSize="11"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Disabled"/>
            </Grid>
        </Border>

    </Grid>
</Window>
